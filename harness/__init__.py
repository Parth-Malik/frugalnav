"""
Test harness -- THROWAWAY scaffolding (plan section 6). None of this ports to
hardware. It exists to exercise the portable `core/` without needing a compiled
VIO, a drone, or a camera: a drift-injection VIO model plus synthetic-but-
physically-consistent ArUco sightings, and plotting.
"""
