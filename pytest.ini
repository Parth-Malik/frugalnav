[pytest]
testpaths = tests
# ros2_ws holds colcon build output (and WSL symlinks Windows can't traverse);
# datasets is large downloaded data. Neither contains tests.
norecursedirs = ros2_ws datasets cpp outputs report web profiling .git
